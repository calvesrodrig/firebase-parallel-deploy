
name: Deploy to Prod

# on:
#   workflow_dispatch:
#   pull_request:
#     branches:
#       - master
#     types: [closed]

on:
  pull_request:
    branches: [master]

env:
  PROJECT: ${{ secrets.PROJECT_ID_TEST }}
  BRANCH: ${{ github.event.pull_request.head.ref }}
  FUNCTIONS_PATH: deploy-functions
  TEST: ( "0" "1" )

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install script dependencies
        working-directory: ./.github/scripts
        run: npm install

      - name: echo branch
        shell: bash
        run: echo ${BRANCH}

      - name: Create functions folder
        working-directory: ./.github/scripts
        run: mkdir ${FUNCTIONS_PATH}

      - name: Download artifact
        working-directory: ./.github/scripts
        env:
          BRANCH: ${{ env.BRANCH }}
          REPOSITORY: ${{ github.repository }}
          FUNCTIONS_PATH: ${{ env.FUNCTIONS_PATH }}
          TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: node getArtifacts.js

      - name: Check folder
        shell: bash
        working-directory: ./.github/scripts
        run: cd ${FUNCTIONS_PATH} && ls -R

      - name: Remove branch invalid char
        shell: bash
        run: |
          RENAMED_BRANCH="${BRANCH////$'-'}"
          echo "RENAMED_BRANCH=$RENAMED_BRANCH" >> $GITHUB_ENV

      - name: Add functions array to env
        run: |
          FUNCTIONS=$(cat .github/scripts/${FUNCTIONS_PATH}/functions-${{ env.RENAMED_BRANCH }}.json)
          echo "FUNCTIONS=$FUNCTIONS" >> $GITHUB_ENV

  example_matrix:
    runs-on: ubuntu-latest
    needs: [deploy]
    strategy:
      matrix:
        function: ["function1", "function2"]
    steps:
      - name: Print
        run: echo ${{ matrix.function }}